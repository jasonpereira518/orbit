import { Skeleton } from "@/components/ui/skeleton";

/**
 * The feedback list: header, the four metric tiles, the filter row, then the entries.
 * Previously inherited `admin/loading.tsx`, which has no tile row, so the metrics dropped in
 * after the fact and pushed the list down.
 */
export default function AdminFeedbackLoading() {
  return (
    <div className="space-y-6">
      <div>
        <Skeleton className="h-8 w-32" />
        <Skeleton className="mt-2 h-4 w-64" />
      </div>
      <div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">
        {Array.from({ length: 4 }).map((_, i) => (
          <Skeleton key={i} className="h-20 rounded-xl" />
        ))}
      </div>
      <Skeleton className="h-8 w-56" />
      <div className="space-y-3">
        {Array.from({ length: 5 }).map((_, i) => (
          <Skeleton key={i} className="h-24 w-full rounded-xl" />
        ))}
      </div>
    </div>
  );
}
