{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run check:env && npm run db:migrate && next build",
  "crons": [
    {
      "path": "/api/imports/process-stalled",
      "schedule": "0 0 * * *"
    }
  ]
}
